###############################################################################
# Copyright (c) Lawrence Livermore National Security, LLC and other
# RAJA Project Developers. See top-level LICENSE and COPYRIGHT
# files for dates and other details. No copyright assignment is required
# to contribute to RAJA.
#
# SPDX-License-Identifier: (BSD-3-Clause)
###############################################################################

###############################################################################
# VARIABLES
###############################################################################

variables:
  # GIT_STRATEGY: fetch
  # GIT_DEPTH: 1
  GIT_SUBMODULE_STRATEGY: normal
  GIT_SUBMODULE_DEPTH: 1
  GIT_SUBMODULE_UPDATE_FLAGS: --jobs 3
  GIT_SUBMODULE_PATHS: blt tpl/camp tpl/desul

###############################################################################
# MAIN PIPELINE STAGES
###############################################################################

stages:
  - build-and-test

###############################################################################
# JOBS
###############################################################################

.common-setup:
  stage: build-and-test
  rules:
    # Direct pushes to the mirrored develop branch.
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "develop"'
      when: always
    # Temporary exception while validating the GitHub status bridge in PR #2080.
    # Remove this rule after the bridge is merged and validated.
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "balos1/add-uoregon-gitlab-ci"'
      when: always
    # GitHub PRs mirrored as branches named pr<number>_<branch>.
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH =~ /^pr[0-9]+(_.*)?$/'
      when: always
    # Native GitLab or externally integrated GitHub PR pipelines targeting develop.
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "develop"'
      when: always
    - if: '$CI_PIPELINE_SOURCE == "external_pull_request_event" && $CI_EXTERNAL_PULL_REQUEST_TARGET_BRANCH_NAME == "develop"'
      when: always
    - when: never
  variables:
    OMP_NUM_THREADS: "8"
    OMP_PLACES: "cores"
    OMP_PROC_BIND: "close"
  before_script:
    - |
      if [ "${CI_PIPELINE_SOURCE}" = "schedule" ]; then
        export BUILD_TYPE=Debug
      elif [ "${CI_COMMIT_REF_NAME}" = "develop" ]; then
        export BUILD_TYPE=Release
      else
        export BUILD_TYPE=RelWithDebInfo
      fi

.junit-artifacts:
  artifacts:
    when: always
    paths:
      - build/junit.xml
      - build/Testing/
    reports:
      junit: build/junit.xml

NVIDIA-RTX5080:
  extends: [.common-setup, .junit-artifacts]
  tags: [uo-gpu, nvidia-rtx5080, x86_64-nvidiagpu]
  image: nvcr.io/nvidia/cuda:13.3.0-devel-ubuntu26.04
  timeout: 2h
  script:
    - apt-get update && apt-get install -y build-essential cmake git python3
    - export CMAKE_BUILD_PARALLEL_LEVEL=4
    - >-
      cmake -S ${CI_PROJECT_DIR} -B build
      -DCMAKE_BUILD_TYPE=${BUILD_TYPE}
      -C ${CI_PROJECT_DIR}/host-configs/uoregon/nvidia_rtx5080.cmake
    - cmake --build build
    - ctest --test-dir build --output-on-failure --output-junit junit.xml -T test

NVIDIA-GH200:
  extends: [.common-setup, .junit-artifacts]
  tags: [uo-gpu, nvidia-gh200, arm64-nvidiagpu]
  image: nvcr.io/nvidia/cuda:13.1.0-devel-ubuntu24.04
  timeout: 2h
  script:
    - apt-get update && apt-get install -y build-essential cmake git python3
    - export CMAKE_BUILD_PARALLEL_LEVEL=8
    - >-
      cmake -S ${CI_PROJECT_DIR} -B build
      -DCMAKE_BUILD_TYPE=${BUILD_TYPE}
      -C ${CI_PROJECT_DIR}/host-configs/uoregon/nvidia_gh200.cmake
    - cmake --build build
    - ctest --test-dir build --output-on-failure --output-junit junit.xml -T test

NVIDIA-GRACE-GRACE:
  extends: [.common-setup, .junit-artifacts]
  tags: [grace, hpsf, hpsf-neoverse_v2, hpsf-aarch64]
  image: ubuntu:latest
  timeout: 2h
  script:
    - apt-get update && apt-get install -y build-essential cmake git python3
    - export CMAKE_BUILD_PARALLEL_LEVEL=8
    - >-
      cmake -S ${CI_PROJECT_DIR} -B build
      -DCMAKE_BUILD_TYPE=${BUILD_TYPE}
      -C ${CI_PROJECT_DIR}/host-configs/uoregon/nvidia_grace_grace.cmake
    - cmake --build build
    - ctest --test-dir build --output-on-failure --output-junit junit.xml -T test

NVIDIA-DGX-SPARK:
  extends: [.common-setup, .junit-artifacts]
  tags: [uo-gpu, nvidia-gb10, aarch64-nvidiagpu]
  image: nvcr.io/nvidia/cuda:13.1.1-devel-ubuntu24.04
  timeout: 2h
  script:
    - apt-get update && apt-get install -y build-essential cmake git python3
    - export CMAKE_BUILD_PARALLEL_LEVEL=8
    - >-
      cmake -S ${CI_PROJECT_DIR} -B build
      -DCMAKE_BUILD_TYPE=${BUILD_TYPE}
      -C ${CI_PROJECT_DIR}/host-configs/uoregon/nvidia_dgx_spark.cmake
    - cmake --build build --verbose
    - ctest --test-dir build --output-on-failure --output-junit junit.xml -T test

INTEL-DATA-CENTER-MAX-1100:
  extends: [.common-setup, .junit-artifacts]
  tags: [uo-gpu, intel-data-center-max-1100, x86_64-intelgpu]
  image: intel/oneapi:2026.0.0-devel-ubuntu24.04
  timeout: 2h
  script:
    - apt-get update && apt-get install -y build-essential cmake git python3 intel-ocloc
    - sycl-ls
    - export ONEAPI_DEVICE_SELECTOR=level_zero:gpu
    - export CMAKE_BUILD_PARALLEL_LEVEL=8
    - >-
      cmake -S ${CI_PROJECT_DIR} -B build
      -DCMAKE_BUILD_TYPE=${BUILD_TYPE}
      -C ${CI_PROJECT_DIR}/host-configs/uoregon/intel_pvc.cmake
    - cmake --build build
    - >-
      ctest --test-dir build --output-on-failure --output-junit junit.xml -T test
      --exclude-regex 'test-forall-basic-msg-Sycl|test-tensor-register-double-Subtract'

AMD-MI300A:
  extends: [.common-setup, .junit-artifacts]
  tags: [uo-gpu, odyssey, amd-mi300, x86_64-amdgpu]
  image: rocm/dev-ubuntu-26.04:7.14.0-full
  timeout: 2h
  script:
    - apt-get update && apt-get install -y build-essential cmake git python3
    - export CMAKE_BUILD_PARALLEL_LEVEL=8
    - export HSA_XNACK=1
    - export HIP_VISIBLE_DEVICES=3
    - export LD_LIBRARY_PATH=/opt/rocm/lib:/opt/rocm/lib64:${LD_LIBRARY_PATH}
    - >-
      cmake -S ${CI_PROJECT_DIR} -B build
      -DCMAKE_BUILD_TYPE=${BUILD_TYPE}
      -C ${CI_PROJECT_DIR}/host-configs/uoregon/amd_mi300a.cmake
    - cmake --build build
    - ctest --test-dir build --output-on-failure --output-junit junit.xml -T test
